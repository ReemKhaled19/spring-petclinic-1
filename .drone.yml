kind: pipeline
type: docker
name: push-to-dockerhub

steps:
  - name: push-image
    image: plugins/docker
    settings:
      repo:    reemsolimman/spring-petclinic
      username:
        from_secret: reemsolimman
      password:
        from_secret: Reem010..
      tags:
        - latest
        - ${DRONE_BUILD_NUMBER}  # كل build ياخد رقم version مختلف

